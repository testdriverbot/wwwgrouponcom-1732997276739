version: 4.1.29
steps:
  - prompt: Identify the main call to action on the webpage.
    commands:
      - command: hover-text
        text: Sign In
        description: sign in button in the top right of the header
        action: hover
  - prompt: Click the call to action.
    commands:
      - command: hover-text
        text: Use current location
        description: use current location button in the trending section
        action: click
  - prompt: >-
      Assert that the expected page or modal appears.  # Use the content of the
      file from the environment
    commands:
      - command: assert
        expect: www.groupon.com wants to
